# 巴蜀中学竞赛班模拟器（学生版）—— UI 分析报告

> 分析日期：2026-06-29
> 分析范围：主界面布局、视觉设计、交互体验、响应式适配
> 涉及文件：`巴蜀中学竞赛班模拟器_学生版.html`（含内联 CSS/JS）、`css/style.css`、`css/transform.css`

---

## 1. 项目概述

### 1.1 项目基本信息

| 项目 | 说明 |
|------|------|
| **项目名称** | 巴蜀online · 学生版（巴蜀中学竞赛班模拟器） |
| **项目类型** | 网页端文字模拟/养成类游戏（类 Dinkum / 增强版人生模拟器） |
| **技术栈** | 单文件 HTML（~500KB+），内联 CSS + jQuery 3.1.0 + 原生 JS，无构建工具 |
| **目标用户** | 对中学竞赛文化感兴趣的青少年及学生群体，尤其是重庆巴蜀中学在校学生或校友 |
| **核心玩法** | 玩家扮演竞赛班学生，在六年（初一至高三）的学习生活中平衡竞赛训练、文化课学习、伙伴社交和恋爱关系 |

### 1.2 当前 UI 风格定位

当前界面采用**现代 Web 应用风格**，以紫蓝色系（Indigo `#4f46e5`）为主色调，整体偏**仪表盘/Dashboard 类管理工具**的视觉语言。界面包含以下风格特征：

- **暗色/亮色双主题**：通过 `data-theme` 属性切换 CSS 变量实现
- **圆角卡片式面板**：统一使用 `border-radius: 12px` 的卡片容器
- **渐变色按钮与激活态**：大量 `linear-gradient(135deg, ...)` 用于激活按钮、选中状态
- **半透明背景模糊**：Header 使用 `backdrop-filter: blur(8px)` 实现毛玻璃效果
- **像素风 + 现代风混搭**：启动画面引入像素风字体（PixelTitle / GameBody），但主界面使用现代无衬线字体
- **多系统风格并存**：微信风格（伙伴聊天）、原神风格（成就弹窗）、视觉小说风格（恋爱对话）在同一项目中混用

---

## 2. 当前主界面布局分析

### 2.1 整体页面结构

页面由以下层级组成（由外到内）：

```
<body>
  ├── 主题切换按钮（.theme-toggle，fixed 浮动）
  ├── 视觉小说覆盖层（#vn-overlay，z-index: 9999）
  ├── 新手引导系统（#tutorial-system，z-index: 99998~100002）
  ├── 黑客开屏动画（#loader-screen，z-index: 99999，动画完成后隐藏）
  ├── 启动画面（#start，z-index: 10000，选科/选天赋/开始游戏）
  └── 主应用（#app，默认 hidden）
       ├── <header>（48px，sticky）
       └── 三栏内容区（.app，grid 布局）
```

### 2.2 Header（48px 固定顶栏）

Header 使用 `position: sticky; top: 0; z-index: 100`，结构如下：

| 区域 | 内容 | 备注 |
|------|------|------|
| **左侧** | Logo 文本 "巴蜀竞赛班 · 学生版" | 字号 .95em，使用 `var(--pri)` 着色 |
| **中间** | 导航标签组（学习/伙伴/商店/恋爱） | 水平排列，`gap: 4px`，使用 `.nav-tab` 样式 |
| **右侧** | 状态栏（冲刺标记/时间/阶段/资金） | `.si` 标签样式，带左侧 3px 主色边框 |
| **右侧扩展** | 音乐按钮（含下拉面板）、保存/读取/音效按钮 | 紧凑排列，字号 .75em |

Header 中的导航标签不是居中的，而是通过 `margin-left: auto` 靠右放置，紧邻右侧状态栏。这意味着在窄屏下，中间标签组和右侧状态栏会产生横向挤压。

### 2.3 三栏布局（主内容区）

使用 CSS Grid 实现三栏布局：

```css
.app {
  display: grid;
  grid-template-columns: 300px 1fr 1fr;
  gap: 1px;
  max-width: 1400px;
  margin: 0 auto;
  height: calc(100vh - 49px);
}
```

#### 2.3.1 左栏（300px 固定宽度）

左栏包含三个面板，纵向堆叠，内容可滚动：

1. **个人状态面板**（`.panel` > `.panel-h` "个人状态"）
   - 天赋标签展示区（`#talent-tags`）
   - 6 个属性条（智力/精力/心态/压力/竞赛评级/资金）
   - 每个属性条由三部分组成：emoji + 文字标签（`.attr-label`）、线性进度条（`.attr-bar` > `.attr-fill`）、数值（`.attr-val`）
   - 进度条颜色各不相同：智力=主色、精力=成功绿、心态=警告黄、压力=危险红、竞赛评级=警告黄、资金=成功绿

2. **学科能力面板**（`.panel-h` "学科能力"）
   - 使用 4 列 grid 布局（`.subj-grid { grid-template-columns: repeat(4, 1fr) }`）
   - 每个学科格（`.subj-box`）显示：学科名（`.sl`）+ 数值（`.sv`）
   - 数值根据等级变色

3. **成就列表面板**（`.panel-h` "成就"）
   - 显示成就计数（如 "0/20"）
   - 列表区域有 `max-height: 120px; overflow-y: auto` 限制

#### 2.3.2 中栏（1fr 自适应宽度）

中栏包含：

1. **学习模式切换**（两个 `.nav-tab` 按钮，各占 `flex: 1`）
   - "综合学习" / "竞赛学习" 切换
   - 切换通过 `switchStudyTab()` 函数控制面板显隐

2. **综合学习面板**（`#study-cul-panel`）
   - 内含 `.acts` grid 容器（2 列）
   - 行动按钮（`.act-btn`）以 2x2 网格排列
   - 每个按钮包含：图标（`.act-icon`）、标题（`.act-title`）、描述（`.act-desc`）、效果标签组（`.act-effects` > `.act-tag`）

3. **竞赛学习面板**（`#study-comp-panel`，默认隐藏）
   - 结构与综合学习面板相同
   - 独立的行动列表容器

行动按钮的详细结构：
```
.act-btn
  ├── .act-icon     （1.4em emoji 图标）
  ├── .act-title    （0.88em，主色粗体）
  ├── .act-desc     （0.78em，次要色，描述文本）
  └── .act-effects  （flex wrap 容器）
       ├── .act-tag.nrg-down    （精力下降 - 红色系）
       ├── .act-tag.mood-up     （心态上升 - 蓝色系）
       ├── .act-tag.stress-down （压力下降 - 紫色系）
       ├── .act-tag.stat-up     （属性上升 - 绿色系）
       └── ...（共 12 种效果标签样式）
```

#### 2.3.3 右栏（1fr 自适应宽度）

右栏包含：

1. **下月预测卡片**
   - 绿色半透明背景（`rgba(16,185,129,.06)`）
   - 绿色边框，圆角 10px
   - 包含预测标题和内容区域

2. **竞赛日记标题**（h3 "竞赛日记"）
   - 下方的 `.log` 容器承载事件流
   - 每条日志（`.log-e`）包含：时间戳、事件描述、效果数值（`.log-eff`）
   - 底部有 1px 边框分隔线

3. **成绩历程区域**
   - 标题 "成绩历程"
   - 文字内容区域（默认 "暂无考试记录"）
   - 成长曲线图容器（Canvas，500x200）
   - 能力雷达图容器（Canvas，300x250）
   - 两个图表默认 `display: none`，有考试数据后显示

### 2.4 其他页面

#### 伙伴页面（`#friends-page`）

采用**微信桌面版风格**，完全独立于主界面三栏布局：
- 左侧 280px：联系人列表 + "发现新伙伴" 区域
- 右侧 1fr：聊天区域（空状态/对话内容）
- 整体高度 `calc(100vh - 49px)`，与主界面共用同一 Header
- 通过 `switchTab('friends')` 切换时，主三栏 `.app` 被 hidden，伙伴页面显示

#### 恋爱页面（`#love-page`）

- 最大宽度 800px 居中
- 顶部粉色渐变头部（心形图标 + 标题）
- 心形进度条、恋爱阶段徽章
- 默认为锁定状态，需好感度满 100 解锁
- 与伙伴页面一样是独立全屏覆盖

#### 商店

- 以**模态弹窗**（Overlay + Modal）形式展示
- `z-index: 2000`，高于普通弹窗的 9999... 实际上 2000 < 9999，存在层级冲突隐患
- 最大宽度 600px，含分类筛选、每日特价、商品网格

### 2.5 响应式断点与行为

```css
/* 1100px 以下 */
@media(max-width:1100px){
  .app { grid-template-columns: 280px 1fr; }  /* 三栏变两栏 */
  .right { grid-column: 1/-1; max-height: 300px; }  /* 右栏横跨全宽，限高 */
}

/* 700px 以下 */
@media(max-width:700px){
  .app { grid-template-columns: 1fr; }  /* 两栏变单栏 */
  .right { max-height: none; }  /* 右栏不限高 */
}
```

**注意**：Header 中的导航标签和状态栏没有对应的响应式处理。在窄屏下（尤其是 700px 以下），Header 内容会水平溢出或换行挤压，因为缺少 `flex-wrap` 或折叠菜单机制。

---

## 3. 视觉设计分析

### 3.1 配色方案

#### 亮色主题

| 变量 | 色值 | 用途 |
|------|------|------|
| `--pri` | `#4f46e5` (Indigo-600) | 主色：Logo、标题、激活态、进度条 |
| `--pri-l` | `#6366f1` (Indigo-500) | 主色亮：渐变终点、hover 边框 |
| `--pri-d` | `#4338ca` (Indigo-700) | 主色暗：渐变起点（hover 反转） |
| `--sec` | `#374151` (Gray-700) | 次要文字 |
| `--acc` | `#4b5563` (Gray-600) | 辅助文字 |
| `--suc` | `#10b981` (Emerald-500) | 成功/正向反馈 |
| `--dan` | `#ef4444` (Red-500) | 危险/负面反馈 |
| `--wrn` | `#f59e0b` (Amber-500) | 警告/成就 |
| `--bg` | `#f0f2f8` | 页面背景（带渐变） |
| `--card` | `#ffffff` | 卡片背景 |
| `--bdr` | `#e2e8f0` (Slate-200) | 边框色 |

#### 暗色主题

暗色主题采用深蓝紫色调背景渐变（`#0f172a` -> `#1a1f3a` -> `#1e1b4b`），主色反转为浅紫蓝 `#a5b4fc`，卡片使用 `#1e293b`（Slate-800），整体阴影加深。

#### 语义色系统

效果标签（`.act-tag`）共定义了 12 种语义色变体：

| 标签类 | 亮色 | 暗色 | 含义 |
|--------|------|------|------|
| `.nrg-down` | `#dc2626` (红) | `#f87171` | 精力下降 |
| `.nrg-up` | `#16a34a` (绿) | `#4ade80` | 精力上升 |
| `.mood-up` | `#2563eb` (蓝) | `#60a5fa` | 心态上升 |
| `.mood-down` | `#dc2626` (红) | `#f87171` | 心态下降 |
| `.stress-down` | `#9333ea` (紫) | `#c084fc` | 压力下降 |
| `.stress-up` | `#dc2626` (红) | `#f87171` | 压力上升 |
| `.money-down` | `#d97706` (橙) | `#fbbf24` | 资金下降 |
| `.money-up` | `#16a34a` (绿) | `#4ade80` | 资金上升 |
| `.stat-up` | `#059669` (深绿) | `#34d399` | 属性上升 |
| `.stat-down` | `#dc2626` (红) | `#f87171` | 属性下降 |
| `.neutral` | `#64748b` (灰) | `#94a3b8` | 中性 |
| `.charm-up` | `#db2777` (粉) | `#f472b6` | 魅力上升 |
| `.social-up` | `#4f46e5` (靛) | `#818cf8` | 社交上升 |

**分析**：负面效果几乎全部使用红色（nrg-down / mood-down / stress-up / stat-down），无法区分不同类型的负面影响。正面效果虽然使用了不同颜色，但亮度接近，在快速扫视时不够直观。

### 3.2 字体策略

| 字体 | 用途 | 加载方式 |
|------|------|----------|
| `PixelTitle`（Pixeltype.ttf） | 启动画面标题 | `@font-face` 本地加载 |
| `GameBody`（jd_code.ttf） | 全局正文 | `@font-face` 本地加载 |
| `'Segoe UI'` | Windows 系统回退 | 系统字体 |
| `'Microsoft YaHei'` | 中文回退 | 系统字体 |

**分析**：
- 全局字体声明 `* { font-family: 'GameBody', ... }` 使用游戏像素风字体作为默认字体，这是一个非常规的选择。像素字体在正文字号（15px）和大量文字阅读场景下，可读性不如常规无衬线字体
- 启动画面使用 `PixelTitle` 用于标题，搭配 `letter-spacing: .08em` 效果较好
- 未使用 Google Fonts 或 CDN 字体，全部依赖本地 ttf 文件

### 3.3 间距与节奏

| 元素 | 间距值 | 规律 |
|------|--------|------|
| Header 高度 | 48px | 固定值 |
| 左栏宽度 | 300px | 固定值 |
| 面板内 padding | 10px 14px | 统一 |
| 面板标题 padding | 10px 14px | 与 body 一致 |
| 行动按钮 padding | 14px 16px | 略大于面板 |
| 行动按钮间距 | 6px (gap) | 紧凑 |
| 属性行间距 | 6px 0 (padding) | 紧凑 |
| grid gap | 1px (三栏) / 6px (行动按钮) / 1px (学科 grid) | 不统一 |

**分析**：整体间距节奏不一致。三栏之间用 `gap: 1px`（几乎是零间距，靠背景色充当分隔线），行动按钮 `gap: 6px`，学科 grid 也是 `gap: 1px`。1px 的间距视觉上看起来像是边框而不是间距，这导致面板之间缺乏"呼吸感"。

### 3.4 卡片与层级

卡片层级通过以下手段建立：

1. **阴影层级**：
   - 默认：`0 2px 8px rgba(30,41,59,.08)`
   - hover：`0 8px 30px rgba(30,41,59,.12)`（shadow-lg）
   - glow：`0 0 20px rgba(79,70,229,.15)`

2. **背景层级**：
   - 页面背景（`--bg`） > 卡片背景（`--card`） > 次级卡片背景（`--card2`） > 边框色（`--bdr`）

3. **z-index 层级**（存在数值混乱）：
   - Header：`z-index: 100`
   - 模态弹窗：`z-index: 9999`
   - 商店/考试/补习等模态：`z-index: 2000`（反而低于普通弹窗！）
   - 视觉小说：`z-index: 9999`（与普通弹窗同级）
   - 新手引导：`z-index: 99998 ~ 100002`
   - 开屏动画：`z-index: 99999`

**问题**：z-index 数值缺乏规划，商店模态的 `z-index: 2000` 低于普通事件弹窗的 `z-index: 9999`，可能导致弹窗遮挡顺序异常。

### 3.5 动效使用

| 动效 | 类型 | 触发场景 |
|------|------|----------|
| `fadeIn / fadeOut` | opacity + scale | 开屏动画中的命令行窗口 |
| `lockShatter` | scale + rotate + brightness + opacity | 锁裂解效果 |
| `widthing` | width 从 0 到 114px | 开屏进度条 |
| `pulse` | opacity 1 到 0.5 循环 | 冲刺标记闪烁 |
| `typingBounce` | scale 弹跳 | 伙伴打字指示器 |
| `bondGlow` | box-shadow 呼吸 | 羁绊等级光效 |
| `achSlideIn / achIconPop / achStarTwinkle` | 组合动效 | 成就达成弹窗 |
| `loveSlideIn / loveHeartBeat` | 组合动效 | 恋爱解锁弹窗 |
| `vnCharAppear / vnCursorBlink / vnBounce` | 逐字显示 + 光标 | 视觉小说对话 |
| `unlockPop` | 上滑淡入 | 伙伴成长解锁提示 |
| `tutorialPop` | scale + translateY | 新手引导气泡 |

**分析**：动效数量丰富，但大多集中在弹窗和通知场景。主要的操作区域（行动按钮、属性变化）缺少微交互反馈。例如，行动执行后属性数值变化没有动画过渡（虽然 `.attr-fill` 的 `width` 有 `transition: .5s`，但数值文本变化是瞬间的）。

---

## 4. 存在的问题与不足

### 4.1 布局方面

#### 4.1.1 三栏信息密度不均

左栏固定 300px 承载了大量信息（6 个属性条 + 学科 4x grid + 成就列表），信息密度极高，容易造成视觉拥挤。中栏虽然有与右栏同等的宽度（各 `1fr`），但实际只展示了 2 个切换标签 + 2x2 的行动按钮网格，内容稀疏，显得空旷。

**具体表现**：
- 中栏的行动按钮在宽屏下被拉伸到约 500px 宽度，但内容（图标 + 标题 + 描述 + 标签）只占左侧约 60%，右侧大片留白
- 左栏 300px 中要展示 6 个属性条，每个条目包含 emoji + 标签 + 进度条 + 数值，在 300px 宽度下显得非常紧凑

#### 4.1.2 Header 导航与状态栏在窄屏下挤压

Header 中的导航标签组（学习/伙伴/商店/恋爱）和右侧状态栏（时间/阶段/资金/音乐/保存/读取/音效）在 48px 高度内水平排列。在 1100px 以下断点处：
- 三栏变两栏，但 Header 没有对应的响应式调整
- 导航标签 + 状态栏的总宽度可能超过屏幕宽度
- 缺少汉堡菜单或折叠机制

#### 4.1.3 伙伴页面与主界面割裂

伙伴页面（`#friends-page`）是一个完全独立的全屏覆盖层，`switchTab('friends')` 时直接隐藏主三栏（`.app`）并显示伙伴页面。这种实现方式：
- 用户在伙伴页面看不到自己的属性状态（左栏被隐藏）
- 返回学习页面时上下文不连续
- 伙伴页面的 Header 导航仍显示，但实际只有"学习"标签是回到主界面的入口

#### 4.1.4 缺少全局侧边导航的统一感

当前导航仅存在于 Header 中间的一排标签按钮，缺乏全局导航的视觉存在感。用户无法一目了然地看到所有可用功能模块。尤其在伙伴和恋爱页面中，Header 的标签指示不够明显，用户可能不知道当前处于哪个页面。

### 4.2 视觉方面

#### 4.2.1 颜色标签（act-tag）过多导致视觉噪音

每个行动按钮下方都有 `.act-effects` 区域，包含 3~5 个 `.act-tag`。这些小标签各用不同颜色（红/绿/蓝/紫/橙/粉），在 2x2 网格中密集排列时：
- 一个屏幕可见 4 个行动按钮，每个有 3~5 个彩色标签，总计 12~20 个彩色元素
- 颜色过于多样，失去语义区分的意义
- 标签字号仅 `.72em`（约 10.8px），在密集排列下可读性差

#### 4.2.2 属性条使用 emoji 前缀降低专业感

6 个属性条均以 emoji 作为前缀（`🧠 智力`、`💪 精力`、`😊 心态`、`😰 压力`、`🏆 竞赛评级`、`💰 资金`）。虽然 emoji 增加了趣味性，但也：
- 在不同操作系统/浏览器中渲染不一致（Windows / macOS / Android 的 emoji 样式差异大）
- 与整体偏专业的 Dashboard 风格不匹配
- 占用了宝贵的标签宽度（300px 窄栏中的水平空间）

#### 4.2.3 面板标题全部使用相同样式缺乏层级区分

所有面板标题（`.panel-h`）使用完全相同的样式：
```css
.panel-h {
  padding: 10px 14px;
  font-size: .82em;
  font-weight: 600;
  color: var(--pri);
  border-bottom: 1px solid var(--bdr);
  /* 带有左侧渐变背景 */
}
```
"个人状态"、"学科能力"、"成就"、"综合学习"、"竞赛学习" 五个面板标题视觉权重完全相同，用户需要阅读文字才能区分不同面板，缺少视觉层级的引导。

#### 4.2.4 商店/恋爱交互模式不一致

| 模块 | 交互模式 | z-index |
|------|----------|---------|
| 商店 | 模态弹窗 Overlay | 2000 |
| 考试 | 模态弹窗 Overlay | 2000 |
| 补习班 | 模态弹窗 Overlay | 2000 |
| 伙伴 | 全屏页面替换 | - |
| 恋爱 | 全屏页面替换 | - |
| 事件 | 模态弹窗 Overlay | 9999 |

商店和考试使用模态弹窗（在当前页面上方弹出），但伙伴和恋爱使用全屏页面替换（隐藏当前页面）。这种不一致会让用户困惑——"为什么点商店是在当前页弹窗，但点伙伴却跳到了另一个完全不同的界面？"

### 4.3 交互方面

#### 4.3.1 缺少视觉焦点引导

新玩家首次进入主界面后：
- 没有明显的"下一步应该做什么"提示（虽然有新手引导系统，但需要额外点击激活）
- 四个行动按钮视觉权重相同，没有高亮推荐操作
- 中栏上方有"综合学习/竞赛学习"切换标签，但没有引导玩家应该先看哪个
- 右栏的"下月预测"和"竞赛日记"可能显示为空或默认内容

#### 4.3.2 行动按钮的反馈不够明确

玩家点击行动按钮后：
- 按钮有 `:active` 伪类反馈（`transform: translateY(0); box-shadow: none`），但仅持续按压瞬间
- 属性条（`.attr-fill`）有 `transition: width .5s` 动画，但数值文本（`.attr-val`）是直接替换
- 没有执行结果摘要——玩家需要自己去左栏对比前后数值差异
- 没有属性变化的浮动数字动画（+5 智力、-10 精力之类的飘字效果）

#### 4.3.3 日志区域信息流太平，缺乏视觉节奏

竞赛日记（`.log`）中每条日志（`.log-e`）样式完全相同：
```css
.log-e {
  margin-bottom: 3px;
  padding: 6px 10px;
  border-bottom: 1px solid var(--bdr);
  font-size: .78em;
  line-height: 1.5;
  color: var(--sec);
}
```
所有日志条目都是统一的小号灰色文字 + 底部分隔线，没有：
- 时间线视觉（左侧竖线 + 节点图标）
- 事件类型图标区分（学习/考试/社交/特殊事件）
- 正面/负面事件的色彩区分
- 重要性层级（重要事件 vs 日常事件）

---

## 5. 主界面重新设计建议

### 5.1 布局重构方案：侧栏导航 + 内容区

建议将当前的"Header 标签导航 + 三栏内容"重构为经典的**侧栏导航 + 右侧内容区**布局。

#### 目标结构

```
+-------------------------------------------------------+
| Header (48px)                                          |
| [Logo]              [状态指标区]           [快捷操作]    |
+------+------------------------------------------------+
| 侧栏  |  内容区（根据当前标签切换）                        |
| 72px  |                                               |
|       |                                               |
| [概览]|   概览页：卡片式 Dashboard                       |
| [学习]|     ┌──────┐ ┌──────┐ ┌──────┐                |
| [伙伴]|     │属性卡 │ │预测卡 │ │事件卡 │                |
| [商店]|     └──────┘ └──────┘ └──────┘                |
| [恋爱]|                                               |
|       |   学习页：宽版操作面板                           |
| [设置]|     ┌─────────────────────────┐               |
|       |     │     行动卡片 (横向宽版)    │               |
|       |     └─────────────────────────┘               |
|       |                                               |
+------+------------------------------------------------+
| 底部快捷栏（保存/音乐/主题）                              |
+-------------------------------------------------------+
```

#### 具体设计参数

| 元素 | 尺寸 | 说明 |
|------|------|------|
| 侧栏宽度 | 64~72px（收起）/ 200px（展开） | 图标+文字，可折叠 |
| Header 高度 | 48px（保持） | 释放标签空间给状态信息 |
| 内容区 | `calc(100vw - 72px)` | 自适应填充 |
| 底部快捷栏 | 40px 固定 | 保存/音乐/主题切换 |

### 5.2 具体改进建议

#### 建议一：导航改为侧栏

**问题**：Header 导航标签与右侧状态栏在窄屏下挤压，且导航存在感弱。

**方案**：
- 将"学习/伙伴/商店/恋爱/设置"从 Header 移至左侧固定侧栏
- 侧栏使用图标 + 文字标签，纵向排列
- 添加"概览"标签作为默认首页，展示综合 Dashboard
- Header 释放空间后可以完整展示时间、阶段、资金等状态指标
- 窄屏下侧栏可折叠为仅图标模式（64px）

#### 建议二：属性展示改为环形图/仪表盘

**问题**：6 个线性属性条在 300px 窄栏中过于拥挤，信息密度高但视觉表现力弱。

**方案**：
- 将属性展示改为 Canvas 绘制的环形进度图（Donut Chart）
- 6 个属性排列为 3x2 的环形图网格
- 每个环形图中心显示数值，圆环颜色对应属性语义色
- hover 时显示属性名称和详细数值
- 这样可以在更小的空间内展示更丰富的信息

#### 建议三：行动卡片加大加宽，添加执行预览

**问题**：中栏 2x2 行动按钮内容稀疏，留白过多；执行后属性变化不直观。

**方案**：
- 行动卡片改为横向宽卡片（单列或两列，而非 2x2）
- 每张卡片增加：
  - 执行后的预计效果预览（小字显示 "+5 智力 / -10 精力"）
  - 执行后的属性变化动画（数值跳动 + 进度条过渡）
  - 不可执行时的灰化原因说明（"精力不足，需要 30 精力"）
- 卡片 hover 时显示详细的执行效果 tooltip

#### 建议四：日志改为时间线风格

**问题**：竞赛日记信息流太平，缺乏视觉节奏。

**方案**：
- 改为左侧竖线 + 圆点节点的时间线布局
- 不同事件类型使用不同颜色节点和图标：
  - 学习事件：蓝色节点 + 书本图标
  - 考试事件：金色节点 + 奖杯图标
  - 社交事件：粉色节点 + 聊天图标
  - 特殊事件：紫色节点 + 星星图标
- 正面事件左 border 为绿色，负面事件为红色
- 重要事件（竞赛获奖、升段等）卡片放大并带高亮边框

#### 建议五：学科能力使用雷达图

**问题**：4x grid 的学科数值展示过于简单，无法直观对比各科水平。

**方案**：
- 使用 Canvas 绘制五边形雷达图
- 五个顶点分别代表五大学科
- 面积越大表示综合能力越强
- 当前主攻竞赛学科高亮标注
- 保留原始数值在雷达图下方作为辅助信息

#### 建议六：添加固定底部快捷栏

**问题**：保存、音乐、主题切换等工具性操作散落在 Header 右侧，占用空间且不够显眼。

**方案**：
- 添加 40px 高度的固定底部栏
- 包含：保存按钮、读取按钮、音乐开关、音效开关、主题切换、设置入口
- 在游戏过程中始终可见，不遮挡主内容区
- 与移动端 App 的底部导航栏概念一致

#### 建议七：统一弹窗风格

**问题**：商店是模态弹窗、恋爱是独立页面、考试是模态弹窗，交互不一致。

**方案**（二选一）：

**方案 A - 全部改为模态弹窗**：
- 伙伴系统也改为模态弹窗（全屏弹窗，背景不隐藏）
- 恋爱系统也改为模态弹窗
- 所有弹窗统一 z-index 层级（建议 9000~9999 范围内分配）

**方案 B - 全部改为独立页面**（推荐）：
- 商店从模态弹窗改为侧栏导航下的独立页面
- 考试结果在独立页面中展示（或使用右侧抽屉面板）
- 统一使用侧栏导航切换，所有功能模块地位平等

### 5.3 z-index 层级规范建议

| 层级 | z-index 范围 | 用途 |
|------|-------------|------|
| 基础内容 | 1~99 | Header、侧栏、内容区 |
| 工具提示 | 100~999 | 下拉面板、tooltip |
| 普通弹窗 | 1000~1999 | 商店、考试、补习、礼物等 |
| 重要弹窗 | 2000~2999 | 事件弹窗、确认对话框 |
| 全屏覆盖 | 3000~3999 | 视觉小说、伙伴聊天详情 |
| 系统级 | 9000~9999 | 新手引导、开屏动画 |
| 最高层 | 10000+ | Toast 通知、成就解锁 |

---

## 6. 优先级排序

### P0 — 高优先级（影响核心体验，建议立即处理）

| 编号 | 改进项 | 影响范围 | 实施难度 | 说明 |
|------|--------|----------|----------|------|
| P0-1 | **行动执行后的属性变化反馈** | 交互体验 | 中 | 当前执行行动后属性变化几乎无感知，需要添加数值跳动动画和飘字效果。这是游戏核心循环（选择行动 -> 查看结果）的关键触点 |
| P0-2 | **z-index 层级混乱修复** | 全局 | 低 | 商店模态 `z-index: 2000` 低于普通事件弹窗 `z-index: 9999`，可能导致弹窗遮挡异常。需要重新规划并统一 |
| P0-3 | **Header 窄屏适配** | 响应式 | 低 | 700px 以下导航标签和状态栏会溢出，需要添加汉堡菜单或标签折叠机制 |
| P0-4 | **日志区域视觉区分** | 信息呈现 | 低 | 为不同类型事件添加图标和色彩区分，提升日志可读性 |

### P1 — 中优先级（提升整体质量，建议下一阶段处理）

| 编号 | 改进项 | 影响范围 | 实施难度 | 说明 |
|------|--------|----------|----------|------|
| P1-1 | **侧栏导航重构** | 布局架构 | 高 | 将 Header 标签导航改为左侧固定侧栏，是最大的架构级改动，但能从根本上解决导航体验和窄屏适配问题 |
| P1-2 | **行动按钮标签简化** | 视觉噪音 | 低 | 减少 `.act-tag` 数量或改为统一的图标标记，降低行动卡片的视觉复杂度 |
| P1-3 | **面板标题层级区分** | 视觉层级 | 低 | 为不同面板标题添加图标前缀或不同的背景色/字号，建立清晰的视觉层级 |
| P1-4 | **学科能力雷达图** | 数据可视化 | 中 | 使用 Canvas 绘制雷达图替代简单的 grid 数值展示，提升信息表现力 |
| P1-5 | **统一弹窗交互模式** | 交互一致性 | 中 | 将伙伴/恋爱/商店统一为同一交互模式（建议全部改为侧栏导航下的独立页面） |
| P1-6 | **新玩家视觉引导** | 引导体验 | 中 | 在首局游戏中高亮推荐行动、添加首次进入指引箭头 |

### P2 — 低优先级（锦上添花，可长期规划）

| 编号 | 改进项 | 影响范围 | 实施难度 | 说明 |
|------|--------|----------|----------|------|
| P2-1 | **属性环形图** | 视觉表现 | 中 | 将线性属性条改为环形进度图，需要引入 Canvas 绘制 |
| P2-2 | **emoji 前缀替换为图标** | 视觉一致性 | 低 | 将属性条/导航标签中的 emoji 替换为统一的 SVG 图标或图标字体 |
| P2-3 | **底部固定快捷栏** | 操作便利性 | 中 | 添加底部固定栏承载工具性操作 |
| P2-4 | **字体策略优化** | 可读性 | 低 | 考虑将正文默认字体从游戏像素风改为常规无衬线字体，仅在标题/品牌处使用像素风 |
| P2-5 | **间距系统统一** | 视觉一致性 | 低 | 建立 4px/8px/12px/16px 的间距 token，统一全站间距节奏 |
| P2-6 | **伙伴页面融入整体布局** | 架构一致性 | 高 | 将伙伴聊天从独立全屏覆盖改为侧栏导航下的内容区，与主界面共享左栏属性面板 |

---

## 附录：关键 CSS 变量清单

```css
:root {
  /* 主色系 */
  --pri: #4f46e5;       /* Indigo-600，主色 */
  --pri-l: #6366f1;     /* Indigo-500，主色亮 */
  --pri-d: #4338ca;     /* Indigo-700，主色暗 */

  /* 中性色 */
  --sec: #374151;       /* Gray-700，次要文字 */
  --acc: #4b5563;       /* Gray-600，辅助文字 */

  /* 语义色 */
  --suc: #10b981;       /* Emerald-500，成功 */
  --dan: #ef4444;       /* Red-500，危险 */
  --wrn: #f59e0b;       /* Amber-500，警告 */

  /* 背景与卡片 */
  --bg: #f0f2f8;        /* 页面背景 */
  --bg2: #e8ebf5;       /* 次级背景 */
  --card: #ffffff;       /* 卡片背景 */
  --card2: #f8faff;      /* 次级卡片背景 */

  /* 文字与边框 */
  --txt: #1e293b;        /* 主文字 */
  --bdr: #e2e8f0;        /* 主边框 */
  --bdr2: #cbd5e1;       /* 次级边框 */

  /* 阴影 */
  --shadow: 0 2px 8px rgba(30,41,59,.08);
  --shadow-lg: 0 8px 30px rgba(30,41,59,.12);
  --glow: 0 0 20px rgba(79,70,229,.15);
}
```

---

> 报告完毕。以上分析基于对 `巴蜀中学竞赛班模拟器_学生版.html` 源码的逐行阅读，涵盖全部内联 CSS（约 545 行）、HTML 结构（约 970 行）及关键 JS 逻辑。建议按 P0 -> P1 -> P2 的优先级逐步实施改进。
